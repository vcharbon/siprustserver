# Rust build artifacts
/target
**/target

# ABNF fuzz output (regenerated on demand via `cargo run -p xtask -- abnf-regen`)
/tmp/abnf-fuzz-out

# Editor / OS
*.swp
.DS_Store

# Load-test / SIPp run outputs (never commit logs, traces, stats)
**/results/
*.log
*_errors.log
*_stat.csv
sipp-*.csv

# Vendored reference source snapshot (not part of the build)
/portsource
.worktrees/

# E2E test-management generated run artifacts (committed inputs live in e2e/{cases,checksets,infra,campaigns,schemas})
/e2e/runs/
_TODOS.md
portsource/
.vscode/
