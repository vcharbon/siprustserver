<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Endurance background "short-hold" UAC — EMERGENCY half of the short stream
  (role=short_em): INVITE → 200 → ACK → 30s hold → BYE. No call limiter. The
  hold answers any in-dialog OPTIONS (RFC 3261 §11): the B2BUA keepalive
  interval is 300s and a rebooted worker's reclaim (router::reclaim_all →
  smooth_keepalives) re-spreads restored dialogs' keepalives EARLIER, so even
  a 30s call can receive a probe mid-hold.

  This half carries `Resource-Priority: esnet.0` (below), the canonical
  emergency token `sip_message::is_emergency_request` recognises. The B2BUA
  Tier-3 admission gate (overload.should_admit) therefore ALWAYS admits these,
  even under cpu_starve overload — so they are NEVER shed. The non-emergency
  half (uac-endurance-short-noemerg.xml, role=short_ne) has no such header and
  IS sheddable. Run both at SHORT_CPS/2 each so the overload experiment can
  prove emergency new calls survive while non-emergency new calls get 503'd.

  The 30000ms hold is hardcoded in the recv timeout attribute because
  sipp's `[$variable]` macro only expands in `<send>` CDATA bodies, not
  in attributes. Endurance harness keeps the hold literal here; the
  orchestrator does not pass -set hold_ms.
-->
<scenario name="K8s UAC endurance short-hold — INVITE / 200 / hold / BYE">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      X-Api-Call: {"destination":{"host":"[field0]","port":5060}}
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: K8s endurance short-hold
      Resource-Priority: esnet.0
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="200" rtd="true" rrs="true"/>

  <send>
    <![CDATA[
      ACK [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      [routes]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!-- 30s hold that answers in-dialog OPTIONS (RFC 3261 §11). The recv is
       MANDATORY (sipp rejects an all-optional recv before a send); ontimeout
       reproduces the former bare 30s pause when no keepalive probe arrives.
       An answered probe re-parks and restarts the 30s hold. -->
  <label id="hold"/>
  <recv request="OPTIONS" crlf="true" timeout="30000" ontimeout="hold_done"/>
  <send>
    <![CDATA[
      SIP/2.0 200 OK
      [last_Via:]
      [last_Record-Route:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: sip:sipp@[local_ip]:[local_port]
      Content-Length: 0
    ]]>
  </send>
  <nop next="hold"/>
  <label id="hold_done"/>

  <send retrans="500">
    <![CDATA[
      BYE [next_url] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      [routes]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <recv response="200" crlf="true"/>

  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>
  <CallLengthRepartition value="100, 1000, 10000, 30000, 60000, 120000"/>
</scenario>
