<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Abuse archetype: re-INVITE flood.

  INVITE → 200 → ACK, then loop [field0] re-INVITEs with [field1] ms
  spacing, then BYE. Designed to exercise the per-call message-count
  defense (MAX_MESSAGES_PER_CALL). When the worker fires
  begin-termination, sipp's default_behaviors "bye" handler 200-OKs the
  in-dialog BYE and ends the scenario early — the remaining loop
  iterations are skipped.

  Each launched call consumes one row from
  /scenarios/uac-abuse-reinvite-flood.csv:
    [field0] = reinvite_count   (e.g. 10, 50, 200, 1000)
    [field1] = interval_ms      (e.g. 5, 20, 100)
-->
<scenario name="K8s UAC abuse — re-INVITE flood">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: K8s abuse reinvite-flood
      Resource-Priority: esnet.0
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="200" rtd="true" rrs="true"/>

  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      [routes]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!--
    Re-INVITE loop. sipp 3.7's <assign value=> parses as a literal float
    at scenario-load time — no [fieldN] substitution — so the loop count
    is hardcoded here. Per-call variance comes from [field1] (interval_ms)
    via the <pause> below, which DOES support keyword substitution.
  -->
  <nop>
    <action>
      <!-- Set well above the per-call cap (default 100) so the worker's
           begin-termination fires mid-loop; sipp's default `bye` handler
           then ends the scenario early. The wire load is still bounded
           because the call dies at MAX_MESSAGES_PER_CALL, not at this
           upper bound. -->
      <assign assign_to="reinvite_loop" value="200"/>
    </action>
  </nop>

  <label id="reinvite_loop"/>
  <!-- sipp 3.7 <pause milliseconds=> is parse-time literal; hardcoded. -->
  <pause milliseconds="200"/>
  <!--
    Fire-and-forget re-INVITE. We intentionally don't <recv> the 200 and
    don't ACK: that's the abuse. sipp 3.7 also refuses to validate
    `<recv optional=true>` followed immediately by `<send>` (deadlock-prone
    state machine), so a passive loop body is the only valid shape here.
    `-default_behaviors none` (set per-Job) keeps sipp from auto-200 /
    auto-BYE / aborting on the unsolicited responses that arrive back.
  -->
  <send>
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      [routes]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>
  <nop test="reinvite_loop" next="reinvite_loop"/>

  <send retrans="500">
    <![CDATA[
      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      [routes]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 3 BYE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <recv response="200" optional="true" crlf="true"/>
</scenario>
