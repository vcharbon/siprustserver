<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<!--
  Abuse archetype: ghost call.

  INVITE → 200 → ACK, then the UAC goes silent forever. No BYE, no
  response to in-dialog OPTIONS / re-INVITE.

  Goal: exercise the B2BUA's dead-call detection (TERMINATING_TIMEOUT_MS
  + orphan sweep) and confirm those calls' limiter slots are reclaimed
  without impacting real-call success.

  sipp's -recv_timeout (configured to 16 min by the harness) ends each
  ghost call after the silence threshold; the analyzer counts the
  outcome under ABUSE_STREAM and ignores it for KPI.
-->
<scenario name="K8s UAC abuse — ghost call after ACK">

  <send retrans="500">
    <![CDATA[
      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: K8s abuse ghost-after-ack
      Resource-Priority: esnet.0
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 0
      a=rtpmap:0 PCMU/8000
    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="200" rtd="true" rrs="true"/>

  <send>
    <![CDATA[
      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
      [routes]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Content-Length: 0
    ]]>
  </send>

  <!--
    Ghost: wait for BYE indefinitely. sipp's -recv_timeout (16 min,
    set by the harness) eventually times the call out — that's the
    point. The B2BUA's TERMINATING_TIMEOUT_MS reclaims the dialog
    independently of what sipp does.
  -->
  <recv request="BYE" crlf="true"/>
</scenario>
