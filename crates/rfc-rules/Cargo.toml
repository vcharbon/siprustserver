[package]
name = "rfc-rules"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "The one home for RFC-violation knowledge: obligation rules decided off an observed SIP wire, shared verbatim by the capture-side census (sip-pcap) and the live recorded-trace audit (sip-net) through thin adapters. Depends on sip-message only."

[lints]
workspace = true

[dependencies]
sip-message = { path = "../sip-message" }
serde = { workspace = true }
